---
"@flue/cli": patch
"@flue/runtime": patch
"@flue/sdk": patch
---

Installed packages once again include the bundled Flue documentation, so commands such as `flue docs read guide/sandboxes` work out of the box.